#Projeto GIT
Primeira alteração, utilizar git add para adicionar ao staged.
Adicionando uma segunda linha e um marcador de numero 123
Linhas de Comandos para o GIT
git add
git commit -m "mensagem"
git status
git log
git restore nomearquivo --store hash
git push - envia para repositorio remoto
git pull traz do repositorio remoto para local eja da merge
git fetch traz para o repositorio local mas não da merge
git diff usado apos o git fetch mostra as linhas diferentes que serão trazidas do repositorio remoto. se estiver de acordo usar o git pull.
